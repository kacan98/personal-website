{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional schema reference for frontmatter validation"
    },
    "title": {
      "type": "string",
      "description": "Project title"
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "URL-friendly slug (lowercase, numbers, hyphens only)"
    },
    "image": {
      "type": "string",
      "pattern": "^/images/projects/",
      "description": "Path to project image (must start with /images/projects/)"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Array of technology tags"
    },
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "icon": {
            "type": "string",
            "enum": ["gitHub", "externalLink", "globe", "mail", "instagram", "facebook", "linkedIn", "libraryBooks", "twitter", "translate", "youTube", "pinterest", "reddit", "school", "science", "whatsApp"]
          },
          "iconName": {
            "type": "string",
            "enum": ["gitHub", "externalLink", "globe", "mail", "instagram", "facebook", "linkedIn", "libraryBooks", "twitter", "translate", "youTube", "pinterest", "reddit", "school", "science", "whatsApp"]
          }
        },
        "required": ["title", "url"],
        "anyOf": [
          { "required": ["icon"] },
          { "required": ["iconName"] }
        ],
        "additionalProperties": false
      },
      "minItems": 1,
      "description": "Array of project links"
    },
    "featured": {
      "type": "boolean",
      "description": "Whether this project should be featured"
    },
    "description": {
      "type": "string",
      "description": "Short project summary"
    },
    "order": {
      "type": "number",
      "description": "Optional ordering index for project sorting"
    }
  },
  "required": ["title", "slug", "image", "tags", "links", "featured"],
  "additionalProperties": false
}
